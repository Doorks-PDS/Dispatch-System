Doorks storage audit / patch notes

Confirmed writable stores using shared writable data root:
- calendar jobs / legacy promote flow
- login users
- employees
- CRM customers / contacts
- documents index and generated PDFs (estimates / invoices / signoffs)
- notifications
- timecards
- time off
- parts catalog CRUD
- uploaded admin CSV replacements (tech notes / billable time / customers / contacts)
- trick store

Also updated persistence-related readers:
- Atlas tech notes search now checks persistent data root first
- Manual index lookup now checks persistent data root first

Automated estimate / invoice description support added:
- POST /documents/auto-description
- app.js buttons: Auto Fill Description, Prompt Builder
- fuel surcharge default aligned to $15

Notes:
- parts_list.csv endpoint already uses DATA_DIR from main.py
- forms remain repo-backed read-only by design
- static/template files remain repo-backed by design
